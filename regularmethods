#pattern="[abc]" check for either a b or f

#pattern="[a-k]" check for all alphabets from a to k

#pattern="[a-z]" check for all lower case alphabets

#pattern="[A-Z]" check for all upper case alphabets

#pattern="[a-zA-Z]"check for all alphabets

#pattern="[0-9]" check for all digital

# pattern="[a-zA-Z0-9] check for alphanumeric characters

# pattern="[^0-9] check for all characters other than digits (^symbol exclude the specified patterns)

# pattern="[^a-zA-Z0-9] check foe special characters and space

# pattern="[\s] check for space

# pattern="[^a-zA-Z0-9\s] check only for special characters

# pattern="[a-z]{3}-[a-z]{3} will check with the give limit. ie, here with 3 consecutive occuring

# pattern="([c-h]|[t-z]) check for the alphabets in the range c-h to t-z

# pattern="([a-z]{3}|[0-9]{3}) check for 3 consecutive digit to alphabets in the give text

# + match one or more

# ? optional

# * zero or more